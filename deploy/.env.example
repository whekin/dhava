# Use URL-safe random values; never commit the real file.
POSTGRES_PASSWORD=replace-with-a-long-random-password
API_ACCESS_KEY=replace-with-an-independent-long-random-key
FIREBASE_PROJECT_ID=nakvali-app
# Paste the complete downloaded JSON only into an untracked local .env or a
# locked multiline Coolify runtime variable. Never commit the real value.
FIREBASE_SERVICE_ACCOUNT_JSON={}
PUBLIC_BASE_URL=https://api.example.com

LOG_LEVEL=info
STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=
STRAVA_APP_REDIRECT_URL=nakvali://strava/connected
