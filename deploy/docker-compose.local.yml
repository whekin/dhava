# Development-only additions to the production-shaped Compose stack.
# Coolify uses docker-compose.yml by itself and never publishes this port.

services:
  web:
    ports:
      - "127.0.0.1:${NAKVALI_WEB_PORT:-8081}:80"
  api:
    ports:
      - "127.0.0.1:${NAKVALI_API_PORT:-8080}:8080"
