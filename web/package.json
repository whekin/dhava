{
  "name": "nakvali-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev --host 127.0.0.1",
    "build": "astro build",
    "preview": "astro preview --host 127.0.0.1",
    "check": "astro check && npm run build && node scripts/check-build.mjs",
    "format": "prettier --write .",
    "check:http": "node scripts/check-http.mjs"
  },
  "dependencies": {
    "@fontsource-variable/archivo": "5.3.0",
    "astro": "7.3.3"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "prettier": "3.9.8",
    "prettier-plugin-astro": "1.0.1",
    "typescript": "6.0.3"
  }
}
